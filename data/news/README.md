# Long news bodies

Optional. One Markdown file per news item, named after the item's `slug`
column in `../news.csv`. Most items need only the `summary` field in the CSV;
add a file here when you want several paragraphs — a trip report, a talk
write-up, a paper explainer.

Same formatting subset as project descriptions.
